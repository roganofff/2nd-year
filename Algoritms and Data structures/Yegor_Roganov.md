# 1 задача T(n) = 3T(n - 1) if n > 0 else 1
 <!-- T(n) = 3T(n - 1) = 3(3T(n - 2)) = 3^2*T(n-2)
    => 3^n * T(n - n) = 3^n * 1 = 3^n --> O(3^n)

# 2 задача T(n) = 2T(n - 1) - 1 if n > 0 else 1
<!-- T(n) = 2T(n - 1) = 2(2T(n - 2) - 1) - 1 = 2^2T(n - 2) - 2 - 1
=> 2^n - 2^(n - 1) - ... - 2^0 = 1 --> O(1)

# 3 задача у нас есть вложенные циклы, но во втором цикле есть break, которые полносью прерывает этот цикл. второй цикл получается О(1), а общая сложность - О(n)

# 4 задача for     т.к. for range(n//2, n+1) => O(n/2)=O(n)
#       while 1 т.к. счётчик j мы каждый разу увеличиваем в два раза, то O(log n)
#           while 2 т.к. счётчик k мы каждый разу увеличиваем в два раза, то O(log n)
все вместе они имеют сложность O(n*log^2 n)

# 5 задача for     O(n)
#       while   O(n)
#           while   O(log n)
все вместе O(n^2 * log n)

# 6 задача s = 1, i = 1; s = 3, i = 2; s = 6; i = 3 ...
#   Si = S(i-1) + i = S(i - 2) + (i-1) + i = ... = S0 + (i - n) + (i - n + 1) + ... + i =
#   = 1 + 2 + 3 + 4 + 5 + 6 .... + n = (n(n + 1)) / 2 
O(sqrt(n))

# 7 задача 
#   for O(n)
#       j будет кратно i, когда j = i, 2i, 3i ..., i^2   O(n^2)
#           for k O(n^2)
все вместе O(n^5)

# 8 задача такая же как 6

# 9 задача при n < 5 O(1); при n >= 5 O(n) 

# 10 задача 
# в лучшем случае a == b и цикл не будет выполняться, значит O(1)
# в худшем случае разница между a и b будет очень большой и сложность будет O(sqrt(n))

# 11 задача
# так как i увеличивается на 1, а потом i * i сравнивается с n, то оно быстро достигнет n и сложность будет 
O(log n)

# 12 задача
# шаг каждый раз увеличивается и зависит от i * x, то сложность будет 
O(log n)

# 13 задача
# for i имеет сложность O(n//2)=O(n)
#   for j это тоже самое, потому что n - math.floor(n/2) == math.floor(n/2), значит сложность O(n)
#       for k имеет шаг 2*k, а значит сложность O(log n)
# значит общая 
O(n^2 * log n)

# 14 задача
# for i сложность O(n)
#   for j сложность O(sqrt(n)), потому что шаг увеличивается на 1 каждый раз
# общая сложность 
O(n * sqrt(n))

# 15 задача
# for i сложность O(n//3 + 1) = O(n)
#   for j из-за шага в 4 нам нужно сделать в 4 раза меньше операций => O(n//4) = O(n)
# общая 
O(n^2)

# 16 задача
# while i счётчик i увеличивается каждый раз в два раза, значит O(log n)
#   while j счётчик идёт от n к нулю и при этом делится каждый раз на два, это аналогично первому циклу, O(log n)
# общая 
O(log^2 n) 

# 17 задача
# из-за того что каждый раз j умножается на два, программа выполняется за
O(log n)

# 18 задача
# O(n + m), потому что два цикла: в одном N, а в другом M

# 19 задача
# for i сложность O(n)
#   for j с каждым разом кол-во итераций в цикле меньше на один, поэтому O(sqrt(n))
O(n * sqrt(n))

# 20 задача
# for i выполняется O(n//2) = O(n)
#   for j каждый раз шаг это следующая степень двойки, значит O(log n)
O(n * log n)

# 21 задача
# переменная a тут вообще не к чему
# i идёт от N к 0 и каждую итерацию уменьшается в два раза, значит
O(log n)

# 22 задача
# i идёт от 1 к n и каждую итерацию увеличивается в два раза, значит
O(log n)

# 23 задача
# for i сложность O(n)
#   for j начинает с 1 и идёт до i, i с каждым началом цикла увеличивается на 1 O(sqrt(n))
O(n * sqrt(n))